<main>

	<h2>You are viewing:
		{{tournaments.name}}
	</h2>

<div class="card">
  <div class="card-body">
    <h5 class="card-title">{{tournaments.name}}</h5>
    <p class="card-text">{{tournaments.description}}</p>
    <p class="card-text"><small class="text-muted">{{tournaments.location}}</small></p>
  </div>
  <img class="card-img-bottom" src=".../100px180/" alt="Card image cap">
</div>
	
				
		
					
			
			
